'use strict';

var version = require('./version.js');
var endpointsToMethods = require('./endpoints-to-methods.js');

function restEndpointMethods(octokit) {
  const api = endpointsToMethods.endpointsToMethods(octokit);
  return {
    rest: api
  };
}
restEndpointMethods.VERSION = version.VERSION;
function legacyRestEndpointMethods(octokit) {
  const api = endpointsToMethods.endpointsToMethods(octokit);
  return {
    ...api,
    rest: api
  };
}
legacyRestEndpointMethods.VERSION = version.VERSION;

exports.legacyRestEndpointMethods = legacyRestEndpointMethods;
exports.restEndpointMethods = restEndpointMethods;
//# sourceMappingURL=index.js.map
