'use strict';

var index = require('../../_virtual/index6.js');

var hasRequiredPathKey;

function requirePathKey () {
	if (hasRequiredPathKey) return index.__module.exports;
	hasRequiredPathKey = 1;

	const pathKey = (options = {}) => {
		const environment = options.env || process.env;
		const platform = options.platform || process.platform;

		if (platform !== 'win32') {
			return 'PATH';
		}

		return Object.keys(environment).reverse().find(key => key.toUpperCase() === 'PATH') || 'Path';
	};

	index.__module.exports = pathKey;
	// TODO: Remove this for the next major release
	index.__module.exports.default = pathKey;
	return index.__module.exports;
}

exports.__require = requirePathKey;
//# sourceMappingURL=index.js.map
