'use strict';

var git$1 = require('../lib/git.js');

var git = {
  configuration(options) {
    return { commit: git$1.head(options), branch: git$1.branch(options) };
  },
};

module.exports = git;
//# sourceMappingURL=git.js.map
