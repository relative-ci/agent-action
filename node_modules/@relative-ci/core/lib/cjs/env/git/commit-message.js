'use strict';

var childProcess = require('node:child_process');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var childProcess__default = /*#__PURE__*/_interopDefault(childProcess);

function getGitCommitMessage(commit = '') {
    let message = '';
    try {
        message = childProcess__default.default.execSync(`git show -s --format=%B ${commit}`).toString().trim();
    }
    catch (error) {
        console.error('Error reading commit message from git', error);
    }
    return message;
}

exports.getGitCommitMessage = getGitCommitMessage;
//# sourceMappingURL=commit-message.js.map
