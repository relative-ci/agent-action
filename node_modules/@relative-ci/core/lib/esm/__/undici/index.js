import { __exports as undici } from '../../_virtual/index9.js';
import { __require as requireClient } from './lib/dispatcher/client.js';
import { __require as requireDispatcher } from './lib/dispatcher/dispatcher.js';
import { __require as requirePool } from './lib/dispatcher/pool.js';
import { __require as requireBalancedPool } from './lib/dispatcher/balanced-pool.js';
import { __require as requireAgent } from './lib/dispatcher/agent.js';
import { __require as requireProxyAgent } from './lib/dispatcher/proxy-agent.js';
import { __require as requireEnvHttpProxyAgent } from './lib/dispatcher/env-http-proxy-agent.js';
import { __require as requireRetryAgent } from './lib/dispatcher/retry-agent.js';
import { __require as requireErrors } from './lib/core/errors.js';
import { __require as requireUtil } from './lib/core/util.js';
import { __require as requireApi } from './lib/api/index.js';
import { __require as requireConnect } from './lib/core/connect.js';
import { __require as requireMockClient } from './lib/mock/mock-client.js';
import { __require as requireMockAgent } from './lib/mock/mock-agent.js';
import { __require as requireMockPool } from './lib/mock/mock-pool.js';
import { __require as requireMockErrors } from './lib/mock/mock-errors.js';
import { __require as requireRetryHandler } from './lib/handler/retry-handler.js';
import { __require as requireGlobal } from './lib/global.js';
import { __require as requireDecoratorHandler } from './lib/handler/decorator-handler.js';
import { __require as requireRedirectHandler } from './lib/handler/redirect-handler.js';
import { __require as requireRedirectInterceptor } from './lib/interceptor/redirect-interceptor.js';
import { __require as requireRedirect } from './lib/interceptor/redirect.js';
import { __require as requireRetry } from './lib/interceptor/retry.js';
import { __require as requireDump } from './lib/interceptor/dump.js';
import { __require as requireDns } from './lib/interceptor/dns.js';
import { __require as requireFetch } from './lib/web/fetch/index.js';
import { __require as requireHeaders } from './lib/web/fetch/headers.js';
import { __require as requireResponse } from './lib/web/fetch/response.js';
import { __require as requireRequest } from './lib/web/fetch/request.js';
import { __require as requireFormdata } from './lib/web/fetch/formdata.js';
import require$$0 from 'node:buffer';
import { __require as requireFilereader } from './lib/web/fileapi/filereader.js';
import { __require as requireGlobal$1 } from './lib/web/fetch/global.js';
import { __require as requireCachestorage } from './lib/web/cache/cachestorage.js';
import { __require as requireSymbols } from './lib/web/cache/symbols.js';
import { __require as requireCookies } from './lib/web/cookies/index.js';
import { __require as requireDataUrl } from './lib/web/fetch/data-url.js';
import { __require as requireEvents } from './lib/web/websocket/events.js';
import { __require as requireWebsocket } from './lib/web/websocket/websocket.js';
import { __require as requireEventsource } from './lib/web/eventsource/eventsource.js';

var hasRequiredUndici;

function requireUndici () {
	if (hasRequiredUndici) return undici;
	hasRequiredUndici = 1;

	const Client = requireClient();
	const Dispatcher = requireDispatcher();
	const Pool = requirePool();
	const BalancedPool = requireBalancedPool();
	const Agent = requireAgent();
	const ProxyAgent = requireProxyAgent();
	const EnvHttpProxyAgent = requireEnvHttpProxyAgent();
	const RetryAgent = requireRetryAgent();
	const errors = requireErrors();
	const util = requireUtil();
	const { InvalidArgumentError } = errors;
	const api = requireApi();
	const buildConnector = requireConnect();
	const MockClient = requireMockClient();
	const MockAgent = requireMockAgent();
	const MockPool = requireMockPool();
	const mockErrors = requireMockErrors();
	const RetryHandler = requireRetryHandler();
	const { getGlobalDispatcher, setGlobalDispatcher } = requireGlobal();
	const DecoratorHandler = requireDecoratorHandler();
	const RedirectHandler = requireRedirectHandler();
	const createRedirectInterceptor = requireRedirectInterceptor();

	Object.assign(Dispatcher.prototype, api);

	undici.Dispatcher = Dispatcher;
	undici.Client = Client;
	undici.Pool = Pool;
	undici.BalancedPool = BalancedPool;
	undici.Agent = Agent;
	undici.ProxyAgent = ProxyAgent;
	undici.EnvHttpProxyAgent = EnvHttpProxyAgent;
	undici.RetryAgent = RetryAgent;
	undici.RetryHandler = RetryHandler;

	undici.DecoratorHandler = DecoratorHandler;
	undici.RedirectHandler = RedirectHandler;
	undici.createRedirectInterceptor = createRedirectInterceptor;
	undici.interceptors = {
	  redirect: requireRedirect(),
	  retry: requireRetry(),
	  dump: requireDump(),
	  dns: requireDns()
	};

	undici.buildConnector = buildConnector;
	undici.errors = errors;
	undici.util = {
	  parseHeaders: util.parseHeaders,
	  headerNameToString: util.headerNameToString
	};

	function makeDispatcher (fn) {
	  return (url, opts, handler) => {
	    if (typeof opts === 'function') {
	      handler = opts;
	      opts = null;
	    }

	    if (!url || (typeof url !== 'string' && typeof url !== 'object' && !(url instanceof URL))) {
	      throw new InvalidArgumentError('invalid url')
	    }

	    if (opts != null && typeof opts !== 'object') {
	      throw new InvalidArgumentError('invalid opts')
	    }

	    if (opts && opts.path != null) {
	      if (typeof opts.path !== 'string') {
	        throw new InvalidArgumentError('invalid opts.path')
	      }

	      let path = opts.path;
	      if (!opts.path.startsWith('/')) {
	        path = `/${path}`;
	      }

	      url = new URL(util.parseOrigin(url).origin + path);
	    } else {
	      if (!opts) {
	        opts = typeof url === 'object' ? url : {};
	      }

	      url = util.parseURL(url);
	    }

	    const { agent, dispatcher = getGlobalDispatcher() } = opts;

	    if (agent) {
	      throw new InvalidArgumentError('unsupported opts.agent. Did you mean opts.client?')
	    }

	    return fn.call(dispatcher, {
	      ...opts,
	      origin: url.origin,
	      path: url.search ? `${url.pathname}${url.search}` : url.pathname,
	      method: opts.method || (opts.body ? 'PUT' : 'GET')
	    }, handler)
	  }
	}

	undici.setGlobalDispatcher = setGlobalDispatcher;
	undici.getGlobalDispatcher = getGlobalDispatcher;

	const fetchImpl = requireFetch().fetch;
	undici.fetch = async function fetch (init, options = undefined) {
	  try {
	    return await fetchImpl(init, options)
	  } catch (err) {
	    if (err && typeof err === 'object') {
	      Error.captureStackTrace(err);
	    }

	    throw err
	  }
	};
	undici.Headers = requireHeaders().Headers;
	undici.Response = requireResponse().Response;
	undici.Request = requireRequest().Request;
	undici.FormData = requireFormdata().FormData;
	undici.File = globalThis.File ?? require$$0.File;
	undici.FileReader = requireFilereader().FileReader;

	const { setGlobalOrigin, getGlobalOrigin } = requireGlobal$1();

	undici.setGlobalOrigin = setGlobalOrigin;
	undici.getGlobalOrigin = getGlobalOrigin;

	const { CacheStorage } = requireCachestorage();
	const { kConstruct } = requireSymbols();

	// Cache & CacheStorage are tightly coupled with fetch. Even if it may run
	// in an older version of Node, it doesn't have any use without fetch.
	undici.caches = new CacheStorage(kConstruct);

	const { deleteCookie, getCookies, getSetCookies, setCookie } = requireCookies();

	undici.deleteCookie = deleteCookie;
	undici.getCookies = getCookies;
	undici.getSetCookies = getSetCookies;
	undici.setCookie = setCookie;

	const { parseMIMEType, serializeAMimeType } = requireDataUrl();

	undici.parseMIMEType = parseMIMEType;
	undici.serializeAMimeType = serializeAMimeType;

	const { CloseEvent, ErrorEvent, MessageEvent } = requireEvents();
	undici.WebSocket = requireWebsocket().WebSocket;
	undici.CloseEvent = CloseEvent;
	undici.ErrorEvent = ErrorEvent;
	undici.MessageEvent = MessageEvent;

	undici.request = makeDispatcher(api.request);
	undici.stream = makeDispatcher(api.stream);
	undici.pipeline = makeDispatcher(api.pipeline);
	undici.connect = makeDispatcher(api.connect);
	undici.upgrade = makeDispatcher(api.upgrade);

	undici.MockClient = MockClient;
	undici.MockPool = MockPool;
	undici.MockAgent = MockAgent;
	undici.mockErrors = mockErrors;

	const { EventSource } = requireEventsource();

	undici.EventSource = EventSource;
	return undici;
}

export { requireUndici as __require };
//# sourceMappingURL=index.js.map
