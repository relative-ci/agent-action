import { branch, head } from '../lib/git.js';

var git = {
  configuration(options) {
    return { commit: head(options), branch: branch(options) };
  },
};

export { git as default };
//# sourceMappingURL=git.js.map
