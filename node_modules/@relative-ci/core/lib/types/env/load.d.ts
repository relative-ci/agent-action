import { type IngestParams, type PluginArgs } from '../constants';
export type LoadEnvConfig = {
    includeCommitMessage?: boolean;
};
/**
 * Load and normalize ingest params based on:
 * 1. plugin arguments
 * 2. agent env vars (RELATIVE_CI_*)
 * 3. env-ci fallback
 * 4. computed values
 */
export declare function loadEnv(args: PluginArgs, config?: LoadEnvConfig, logger?: {
    debug: (...data: any[]) => void;
    log: (...data: any[]) => void;
    info: (...data: any[]) => void;
    warn: (...data: any[]) => void;
    error: (...data: any[]) => void;
}): Promise<IngestParams>;
