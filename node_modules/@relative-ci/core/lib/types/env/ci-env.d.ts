import { type Logger } from '../utils';
type GetCiEnvConfig = {
    includeCommitMessage?: boolean;
};
export type CiEnv = {
    isCi: boolean;
    slug?: string;
    service?: string;
    branch?: string;
    baseBranch?: string;
    commit?: string;
    commitMessage?: string;
    pr?: string;
    build?: string;
    buildUrl?: string;
};
/**
 * Load environment variables - fallback to env-ci environment variables
 */
export declare function getCiEnv(config: GetCiEnvConfig, logger: Logger): Promise<CiEnv>;
export declare function isCi(): boolean;
export {};
