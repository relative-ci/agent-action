import { type CiEnv } from 'env-ci';
/**
 * Extract repository slug(owner/repo) from the repo URL
 */
export declare function getSlugFromGitURL(repositoryURL?: string): string | undefined;
/**
 * Resolve repository slug
 */
export declare function getSlug(envVars: CiEnv): string;
