import { type IngestData } from '../constants';
type Artifact = {
    key: string;
    data: unknown;
};
/**
 * Filter artifact data based on the source type and map them by key
 */
export declare function filterArtifacts(artifactsData: Array<Artifact>): IngestData;
export {};
