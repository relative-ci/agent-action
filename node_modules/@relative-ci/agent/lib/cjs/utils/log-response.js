'use strict';

var logger = require('./logger.js');

function logResponse(response, logger$1 = logger.logger) {
    const { res, info, reportUrl } = response;
    if (res?.job?.internalBuildNumber) {
        logger$1.info(`Job #${res.job.internalBuildNumber} done.`);
    }
    if (info?.message?.txt) {
        logger$1.info(info.message.txt);
    }
    if (reportUrl) {
        logger$1.info('View bundle report:', reportUrl);
    }
}

exports.logResponse = logResponse;
//# sourceMappingURL=log-response.js.map
